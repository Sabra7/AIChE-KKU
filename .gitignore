node_modules
.next
out
next-env.d.ts
.DS_Store
*.tsbuildinfo
.vercel

# Claude Code local overrides (machine-specific, never shared)
.claude/settings.local.json

# Written by `next dev` on every run (Next 16's generate-agent-files).
# Tool output, not project content.
AGENTS.md
CLAUDE.md
